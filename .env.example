# Copie para .env.local (não versionado) e preencha.
# Chave gratuita: https://aistudio.google.com/apikey
# Alternativa: cole a chave direto na tela do chat (fica no localStorage).
VITE_GEMINI_API_KEY=
